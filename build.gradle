buildscript {
    ext {
        compileSdk = 36
        minSdk = 34
        targetSdk = 36
        versionCode = 1036
        versionName = "1.0.36"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // ✅ Cocokkan: AGP 8.7.0 butuh Gradle 8.9
        classpath "com.android.tools.build:gradle:8.7.0"
    }
}

allprojects {
    repositories {
        // KOSONG — sudah di settings.gradle
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
